{
  "framework": "nextjs",
  "buildCommand": "node scripts/migrate.js && next build",
  "regions": ["lhr1"],
  "functions": {
    "app/api/webhooks/**": {
      "maxDuration": 60
    },
    "app/api/**": {
      "maxDuration": 30
    }
  }
}
