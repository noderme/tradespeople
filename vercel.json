{
  "framework": "nextjs",
  "buildCommand": "npx supabase db push --project-ref uvaxuteovtfppvenmrvg && next build",
  "regions": ["lhr1"],
  "functions": {
    "app/api/webhooks/**": {
      "maxDuration": 60
    },
    "app/api/**": {
      "maxDuration": 30
    }
  }
}
